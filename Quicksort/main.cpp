#include <iostream>
#define CATCH_CONFIG_MAIN
#include "catch.hpp"
#include "Sort.h"

#ifdef CATCH_CONFIG_MAIN

/*int main() {
    int array[] = {10,7,8,9,1,5};
    int n = sizeof(array) / sizeof(array[0]);
    QuickSort(array, 0, n-1);
    std::cout << "Lista ordenada: ";
    std::cout << impimirLista(array, n);
    return 0;
}*/

#endif